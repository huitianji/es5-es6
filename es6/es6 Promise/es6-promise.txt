//
es6 promise

操作dom操作很慢。是同步操作

异步就是耗时的操作

异步：读取本地的文件。。cpu缓存速度很快。  2级3级 。  再就是缓存  。最后是硬盘。。 硬盘的读取速度是很慢的
网络请求。。几百毫秒跑几百行等等。
//
同步：
javascrpt  dom操作很慢。。。 渲染 。。。很耗时。。同步操作。。

//
回调函数：

//
promise
过一段时间给一个结果

dom/->浏览器上 node上没有
javascrpt  ecmaspcity


//
状态机的概念

有线的状态机，，

Fulfilled 可以理解为成功的状态
Rejected 可以理解为失败的状态
Pending 既不是 Fulfilld 也不是 Rejected 的状态，可以理解为 Promise 对象实例创建时候的初始状态

、、、


协议就是一个约定。。。


















