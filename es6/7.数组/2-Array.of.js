//Array.of  --  一个参数也是字符。两个多个参数也是字符

//Array()/一个参数是长度 。两个参数是字符
let arr = new Array(1, 2);
let arr2 = [1, 2];
let arr3 = new Array(2);
//Array.of()里面都是参数
let arrof = Array.of(2);

console.log(arr,arr3,arrof);

//console.log(new Set([1, 3,  3, 4]))